# GrowMaster branding

`growmaster-carrot.png` is the approved transparent master artwork used to build the desktop, PWA, Android and iPhone icons. Run `scripts/build_brand_assets.py` after replacing it.
